# apps/web

Next.js dashboard shell for a single local operator.

## Responsibilities

- display runtime health
- display market, order, fill, and PnL panels later
- remain read-only by default until operator actions are explicitly added

## Commands

```bash
npm run dev -w @polymarket-bot/web
npm run build -w @polymarket-bot/web
```
